<?xml version="1.0"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.qtproject.example"
    android:installLocation="auto"
    android:versionCode="-- %%INSERT_VERSION_CODE%% --"
    android:versionName="-- %%INSERT_VERSION_NAME%% --">
    <!-- %%INSERT_PERMISSIONS -->
    <!-- %%INSERT_FEATURES -->

    <!-- USB host, for CAT and PTT over a serial adapter.

         required="false" is load-bearing: Play uses uses-feature to
         decide which devices may install an app, and a required USB host
         would hide this one from anything without an OTG port. Receiving
         over acoustic coupling needs no USB at all, so most of what the
         app does would be withheld from most phones to declare a feature
         some of them use. -->
    <uses-feature android:name="android.hardware.usb.host" android:required="false" />
    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:smallScreens="true" />
    <application
        android:name="org.qtproject.qt.android.bindings.QtApplication"
        android:hardwareAccelerated="true"
        android:label="-- %%INSERT_APP_NAME%% --"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:enableOnBackInvokedCallback="false"
        android:allowBackup="true"
        android:fullBackupOnly="false">
        <activity
            android:name="org.qtproject.qt.android.bindings.QtActivity"
            android:configChanges="orientation|uiMode|screenLayout|screenSize|smallestScreenSize|layoutDirection|locale|fontScale|keyboard|keyboardHidden|navigation|mcc|mnc|density"
            android:label="-- %%INSERT_APP_NAME%% --"
            android:launchMode="singleTop"
            android:screenOrientation="unspecified"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

          <meta-data
                android:name="android.app.lib_name"
                android:value="-- %%INSERT_APP_LIB_NAME%% --" />

          <meta-data
                android:name="android.app.arguments"
                android:value="-- %%INSERT_APP_ARGUMENTS%% --" />
        </activity>

        <!-- The listening session lives here, not in the activity.
             stopWithTask="false" is the whole point: swiping the app
             away must not end a reception, because on a phone the
             operator putting the screen away is the normal case rather
             than a shutdown. The microphone type is required from API
             34 up, and the service may then only be started while the
             app is in the foreground, which is why nothing starts it
             but a button press.
             mediaPlayback is the transmit half: an over is 32 to 95 s of
             committed airtime and must not be truncated because the
             activity went away. Half duplex means only one of the two
             types is ever in use at a time, but both are declared for
             the service's whole lifetime rather than re-declared at each
             transition.
             (No double hyphens in here: XML forbids them inside a
             comment, and aapt reports it as a column number.) -->
        <service
            android:name="org.cleverdomain.sstvae.ListenerService"
            android:exported="false"
            android:stopWithTask="false"
            android:foregroundServiceType="microphone|mediaPlayback" />

        <!-- Picking a picture to transmit. Its own activity because an
             activity result comes back to whoever launched it, and
             reaching QtActivity's means subclassing Qt's bindings or
             using its private API. Translucent and title-less: it exists
             to launch an intent and finish, and must never look like a
             screen of this app. exported="false" because nothing outside
             the app has any business starting it. -->
        <activity
            android:name="org.cleverdomain.sstvae.ImagePicker"
            android:exported="false"
            android:excludeFromRecents="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <!-- Collects a persistent USB permission and disappears. See
             UsbAttach.java: without it Android drops the grant on every
             detach, so the operator re-authorises the radio at the start
             of every session. Its own activity rather than a filter on
             QtActivity, because device_filter.xml matches whole vendor
             ranges and plugging in an Arduino must not open a radio app.
             (No double hyphens in here: XML forbids them inside a
             comment, and aapt reports it as a column number.) -->
        <activity
            android:name="org.cleverdomain.sstvae.UsbAttach"
            android:exported="true"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:theme="@android:style/Theme.NoDisplay">
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>
            <meta-data
                android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
                android:resource="@xml/device_filter" />
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.qtprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/qtprovider_paths"/>
        </provider>
    </application>
</manifest>
